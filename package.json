{
  "name": "newone",
  "version": "1.0.0",
  "description": "npm create vite@latest . first command",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix frontend && npm install --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  }
}
